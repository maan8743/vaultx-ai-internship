Token: tokn in ai is the small peice of text  or data that an ai model reads or process it include test emojji etc. 

Context Windows: the amount or information that ai can read or remmember at one time before generating a response.

Temprature: A setting of an ai that controls how creatively it response 
low temp: focused answer.
high temp: veired and creative answer.

Top-p: A setting that controls how many possible words an AI considers when generating an answer.
low top-p:considers fewer likely 
high top-p: more possible words

System prompt: rules and instructions that guide an ai how to response.

Embedding: turning information and data into number so ai can understand and compare its meaning

Hallucinating: when AI gives wrong answer confidently it called hallucinating 

Fine-tunning: giving instruction to ai to resposne better against a specific data.

Inference: when ai use its knowledge to give output .

Base modle:                   intruction-tunned model

>> predicts text               follows intructions 
>> less focused                more focused 
>> require more propmting      easier to use

LLM : An LLM predicts the most likely next token based on patterns learned from its training data.
It does not store facts exactly like a human memory system; its knowledge is represented in learned patterns and parameters.
Because of this, an LLM can sometimes produce information that sounds correct but is actually wrong